#!/usr/bin/node

// Print 3 lines using array of atring and a loop

const lang = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
const len = lang.length;
let i;

for (i = 0; i < len; i++) {
  console.log(lang[i]);
}
